# Cycle: T001 FP Fix P0 — #31 + #34

## Context
Phase 6-1 dogfooding residual P0 false positives.
- #31: TS `expect.soft/element/poll(x).toX()` (~100+ FP in vitest)
- #34: Python `pytest.warns()` (~2 FP in fastapi)

All fixes are assertion.scm pattern additions only. No Rust logic changes.

## Test List
- TC-01: `expect.soft(x).toBe(y)` -> assertion_count >= 1
- TC-02: `expect.element(locator).toHaveText(text)` -> assertion_count >= 1
- TC-03: `expect.poll(() => value).toBe(expected)` -> assertion_count >= 1
- TC-04: `with pytest.warns(DeprecationWarning):` -> assertion_count >= 1
- TC-05: `pytest.warns(UserWarning, match="msg")` -> assertion_count >= 1
- TC-06: Regression — 527 existing tests all PASS

## Progress
- [x] RED: 5 tests created, all failing (assertion_count = 0)
- [x] GREEN: 3 .scm files updated, 532 tests pass
- [x] DOGFOODING: vitest T001=0, fastapi T001=0

### Phase: RED
**Artifacts**: 5 fixture files, 5 test functions (3 TS, 2 Python)
**Metrics**: 5 new tests, all FAIL as expected

### Phase: GREEN
**Artifacts**:
- `crates/lang-typescript/queries/assertion.scm` — `expect.soft/element/poll` pattern
- `crates/lang-python/queries/assertion.scm` — `pytest.warns()` pattern
- `crates/lang-python/queries/error_test.scm` — `pytest.warns()` cross-reference
**Metrics**: 532 tests pass, clippy 0, fmt clean

### Dogfooding Verification
| Project | Lang | T001 Before | T001 After |
|---------|------|-------------|------------|
| vitest | TS | 493 | 0 |
| fastapi | Python | 22 | 0 |
