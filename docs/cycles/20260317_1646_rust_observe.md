---
feature: "Phase 9c — Rust observe (test-to-code mapping)"
cycle: "20260317_1646"
phase: RED
complexity: standard
test_count: 27
risk_level: low
codex_session_id: ""
created: 2026-03-17 16:46
updated: 2026-03-17 16:46
---

# Cycle: Rust observe (test-to-code mapping) (Phase 9c)

## Scope Definition

### In Scope
- [ ] `crates/lang-rust/src/observe.rs` 新規作成 (ObserveExtractor trait 実装)
- [ ] `crates/lang-rust/queries/production_function.scm` 新規作成
- [ ] `crates/lang-rust/queries/import_mapping.scm` 新規作成
- [ ] `crates/lang-rust/queries/re_export.scm` 新規作成
- [ ] `crates/lang-rust/queries/cfg_test.scm` 新規作成
- [ ] `tests/fixtures/rust/observe/` 配下の observe 用フィクスチャ群
- [ ] `crates/lang-rust/src/lib.rs` 編集 (`pub mod observe;` 追加)
- [ ] `crates/cli/src/main.rs` 編集 (`run_observe` に Rust ディスパッチ追加)
- [ ] Layer 0: inline test self-mapping (#[cfg(test)])
- [ ] 出力に `[experimental]` マーカー

### Out of Scope
- PHP observe 実装 (Phase 9d)
- Actix/Axum/Rocket 等のルート抽出
- Cargo workspace クロスクレート import 解決
- `use super::*` の外部テストファイルからの解決
- proc-macro crate のテスト

### Files to Change
- `crates/lang-rust/src/observe.rs` (new)
- `crates/lang-rust/queries/production_function.scm` (new)
- `crates/lang-rust/queries/import_mapping.scm` (new)
- `crates/lang-rust/queries/re_export.scm` (new)
- `crates/lang-rust/queries/cfg_test.scm` (new)
- `tests/fixtures/rust/observe/` (new)
- `crates/lang-rust/src/lib.rs` (edit)
- `crates/cli/src/main.rs` (edit)

## Environment

### Scope
- Layer: `crates/lang-rust` + `crates/cli`
- Plugin: dev-crew:rust-quality (cargo test / clippy / fmt)
- Risk: 40/100 (PASS)

## Test List

### Stem matching
- [ ] RS-STEM-01: `tests/test_foo.rs` → test_stem → `Some("foo")`
- [ ] RS-STEM-02: `tests/foo_test.rs` → test_stem → `Some("foo")`
- [ ] RS-STEM-03: `tests/integration.rs` → test_stem → `Some("integration")`
- [ ] RS-STEM-04: `src/user.rs` → test_stem → `None`
- [ ] RS-STEM-05: `src/user.rs` → production_stem → `Some("user")`
- [ ] RS-STEM-06: `src/lib.rs` → production_stem → `None`
- [ ] RS-STEM-07: `src/mod.rs` → production_stem → `None`
- [ ] RS-STEM-08: `src/main.rs` → production_stem → `None`
- [ ] RS-STEM-09: `tests/test_foo.rs` → production_stem → `None`

### Helper filtering
- [ ] RS-HELPER-01: `build.rs` → is_non_sut_helper → true
- [ ] RS-HELPER-02: `tests/common/mod.rs` → is_non_sut_helper → true
- [ ] RS-HELPER-03: `src/user.rs` → is_non_sut_helper → false
- [ ] RS-HELPER-04: `benches/bench.rs` → is_non_sut_helper → true

### Layer 0 (inline tests)
- [ ] RS-L0-01: `#[cfg(test)] mod tests {}` → detect → true
- [ ] RS-L0-02: `#[cfg(test)]` なし → detect → false
- [ ] RS-L0-03: `#[cfg(not(test))]` のみ → detect → false

### Production function extraction
- [ ] RS-FUNC-01: `pub fn create_user() {}` → name="create_user", is_exported=true
- [ ] RS-FUNC-02: `fn private_fn() {}` → name="private_fn", is_exported=false
- [ ] RS-FUNC-03: `impl User { pub fn save() {} }` → name="save", class_name=Some("User")
- [ ] RS-FUNC-04: `#[cfg(test)] mod tests` 内 → 抽出されない

### Import extraction
- [ ] RS-IMP-01: `use crate::user::User;` → `("user", ["User"])`
- [ ] RS-IMP-02: `use crate::models::user::User;` → `("models/user", ["User"])`
- [ ] RS-IMP-03: `use crate::user::{User, Admin};` → `("user", ["User", "Admin"])`
- [ ] RS-IMP-04: `use std::collections::HashMap;` → skip

### Barrel
- [ ] RS-BARREL-01: `mod.rs` → is_barrel_file → true
- [ ] RS-BARREL-02: `lib.rs` → is_barrel_file → true
- [ ] RS-BARREL-03: `pub mod user;` → extract_barrel_re_exports
- [ ] RS-BARREL-04: `pub use user::*;` → extract_barrel_re_exports

### E2E
- [ ] RS-E2E-01: inline tests → Layer 0 self-map
- [ ] RS-E2E-02: stem match → Layer 1
- [ ] RS-E2E-03: import match → Layer 2
- [ ] RS-E2E-04: helper excluded

### CLI
- [ ] RS-CLI-01: `observe --lang rust .` → 正常実行

## Progress Log

- 2026-03-17 16:46: Cycle doc created, starting RED phase
