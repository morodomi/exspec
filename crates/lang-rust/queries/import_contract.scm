; Rust has no widely-used runtime contract/validation library analogous to
; Pydantic (Python) or Zod (TypeScript). T008 is always INFO for Rust.
; This file intentionally has no query patterns.
; has_any_match() will always return false (no captures = no match).
