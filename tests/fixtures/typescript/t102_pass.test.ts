describe('UserService', () => {
  let db: any;
  let cache: any;

  beforeEach(() => {
    db = {};
    cache = {};
  });

  it('creates user with few fixtures', () => {
    expect(db).toBeDefined();
  });
});
